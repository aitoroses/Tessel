import expect from 'expect';
import Tessel from './tessel';

describe('A Tessel instance', () => {

  var AppState;
  beforeEach(() => AppState = new Tessel());

  it('has private data', () => {
    
  });
});
