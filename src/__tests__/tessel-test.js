import expect from 'expect';
import Tessel from '../tessel';
import Tracker from '../../lib/tracker'

describe('A Tessel instance', () => {

  var store;
  beforeEach(() => store = new Tessel());

  it('has private data', () => {
    debugger
  });
});
